## 2. Existing repo: keep, reuse, archive

**Keep unchanged (local companion):** `reminder/`, `adviser/`, `tests/` (unittest), `scripts/install-*-task.ps1`, `scripts/uninstall-*-task.ps1`, `adviser/server.py`, `adviser/tunnel.py`, `reminder/scan.py`. The full pre-existing suite (`python -m unittest discover -s tests -t .`) must pass on windows-latest in every phase; on ubuntu the 4 known Windows-path failures (two in `tests.test_graph.ToolTests`, two in `tests.test_adviser_cli.CheckTests`) are listed by test id in `docs/platform/tests_skip_linux.txt` (written in Phase 0) and skipped by `scripts/run_legacy_tests.py` (added in Phase 1: `unittest.defaultTestLoader.discover('tests', top_level_dir='.')`, drops the listed ids when `sys.platform != 'win32'`, exits non-zero on failure); `tests/` itself is never edited, and the `C:/` directory the suite creates on Linux is gitignored. Groq stays the LLM for the local companion only.

**Copy and port into the backend with parity tests against `reminder/` fixtures; `reminder/` stays byte-for-byte unchanged except for the removal of `reminder/second-brain.code-workspace` (R-HYG-06), keeps its Groq `function_calling` fallback and is never imported or packaged by the backend:**

| Existing | Port to | Kept / changed |
|---|---|---|
| `reminder/graph.py` `build_investigator`, `VerdictModel`, `MAX_TOOL_CALLS=6`, dangling-tool-call trimming | `backend/src/bridge/integrations/github/investigator.py` (Release 2) | Same ReAct loop; verdict gains `milestone_id`, `on_track`, `blocking_issue`; structured output via the LLM layer (native schema, never forced tool_choice) |
| `reminder/tools.py` `redact()`, `_clip()`, `guarded`, secret-file refusal list | `bridge/integrations/github/tools.py` | Backed by a GitHub App (`contents:read`, `metadata:read`) bound to one repo+ref |
| `reminder/remind.py` `classify()`, `pick_featured()`, `fallback_text()`, `_record()` + `MAX_ATTEMPTS=3`, once-a-day guard, `compose_email()` card, `template_notices()` | `bridge/reminders/policy.py`, `bridge/reminders/compose.py` | `classify` becomes engagement-health rules (`docs/spec/06-feature-modules.md#611-reminders-for-both-sides-reusing-the-reminder-engine`); once-a-day guard becomes a DB unique constraint; HTML card becomes Jinja2; fallback text kept |
| `reminder/notify.py` `DeliveryError(transient)`, `clean_param`, `WA_MARK_LOOKALIKES`, `build_whatsapp_template`, `send_whatsapp` | `bridge/notifications/whatsapp.py` | Async httpx; Gmail SMTP dropped for `EmailProvider` |
| `adviser/whatsapp.py` HMAC signature check, webhook parsing | `bridge/notifications/whatsapp_webhook.py` | Same `X-Hub-Signature-256` check |
| `adviser/memory.py` `first_time`, `note_inbound` (24h window) | Tables `wa_inbound`, `wa_contacts` | JSON → Postgres |
| `adviser/brain.py` `ModelPool`, `_cooldown`, `propose_reminder_change` | `bridge/llm/pool.py`; "agent proposes → human confirms" generalised to every state change | |
| `adviser/turn.py` `yes_or_no`, `confirm_proposal` | `bridge/adviser/confirm.py` (v1.1) | WhatsApp confirmations |

**Archive** in one `git mv` commit into `legacy/` with a README ("unmaintained, kept for reference"): `n8n/`, `docker-compose.yml` (n8n/Evolution), `notes/` Obsidian templates, `scripts/smoke-test.*`, `scripts/test-pipeline.mjs`, `docs/01–09`, `docs/10-evening-checkin.md`. **Fix hygiene** R-HYG-01 channel flip-flop in docs/README; 02 stale `EVOLUTION_*` vars; 03 retired `llama-3.3-70b-versatile`; 04 `TZ=Africa/Lagos` → `Africa/Nairobi`; 05 duplicate `docs/10-*` numbering; 06 delete `reminder/second-brain.code-workspace`. Keep the root `.env.example` as the documented template for the `.env` that `reminder/remind.py` and `adviser/__main__.py` load, trimmed to the variables they read (GMAIL_*, REMINDER_EMAIL_TO, GROQ_*, WA_*, ADVISER_*) with `GROQ_MODEL` set to a current model (e.g. `openai/gpt-oss-20b`, the `graph.py` default; R-HYG-03 fixes the example value, not code), and add `backend/.env.example`, `frontend/.env.example`.

**Local companion v1.1 (optional):** `python -m reminder --platform` authenticates with a scoped personal token and merges the developer's platform milestones into the local morning email. Local folders are never uploaded.
