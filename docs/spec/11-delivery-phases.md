## 11. Delivery phases with exit criteria (MVP first)

| Phase | Scope | Exit criteria |
|---|---|---|
| **0 Discovery & plan** (default mode, docs only, Fable `max`) | Read the whole repo. Write `PLAN.md`, `REQUIREMENTS.md` (one row per R-id/AC with Given/When/Then, priority, phase, release R1/R2/R3, code paths, tests, status; the N01–N23 matrix), ADR-001..008 (`docs/spec/13-open-decisions.md`), `THREAT_MODEL.md` (STRIDE on auth, tenancy, provenance, payments, idea leakage, injection), `.claude/agents/*.md`, `CLAUDE.md` (<200 lines), `GATES.md`, `DECISIONS-NEEDED.md`, `tests_skip_linux.txt`. | Appendix A (`docs/spec/appendix-a-traceability.md`) is the authoritative decomposition of the original request: every R-id maps to ≥1 REQ-ID and ≥1 AC; no phase exit criterion depends on a later phase (an AC clause that names a later phase's feature is asserted at that phase's exit, as listed below); human signs G0. |
| **1 Hygiene & foundation** | R-HYG-01..06; `legacy/` move; `scripts/run_legacy_tests.py`; scaffold `backend/`, `frontend/`, `infra/`; compose stack; CI `make check`; auth, orgs, roles, RLS, audit log, consents; `plans.yaml` placeholders + entitlement middleware; `EmailProvider` + Mailpit; ported copies of `classify`/`fallback_text`/policy under `bridge/reminders` with parity tests, `reminder/` untouched. | CI green incl. pre-existing tests (windows full, ubuntu skip list); AC-HYG-01..06; AC-SEC-1 on the tables that exist (full re-run at Phase 2 exit); signup/login E2E passes. |
| **2 Repository, directory, provenance** | Problems (admin- and developer-created) and Problem Briefs; proposals with Tier 1/2 and `proposal_confidential`; disclosure records, certificates, `/verify`; NDA-gated unlock; watermarked renders; access log; provisional directory seed (`docs/spec/06-feature-modules.md#62-company-directory-by-niche-claimed-and-unclaimed`; G6 for production) with E0/E1/E2 claim flow; multi-tagging with held tags; originality check; sanitiser + moderation holds; EM1 template. | AC-SEC-1, AC-REPO-*, AC-DIR-*, AC-PROP-1/2/4/5/6/7, AC-IP-1/2/4/5/6/9 pass, except clauses owned by later phases (engagement creation in AC-PROP-1 → Phase 3; AC-REPO-4 digest item and AC-DIR-5 scout form → Phase 4; AC-REPO-6 trending badges → Phase 5); ux-reviewer runs on every frontend PR from here on. |
| **3 Core loop: tracker, approval email, reminders** | Pitch-to-company composer; full state machine incl. stage 0, procurement route, milestones, agreements with internal e-signature and "signed outside", disputes skeleton, Messages; notifications N01–N23 (except N02) and EM1–EM8 (except EM3); developer daily reminder and enterprise progress digest; test clock endpoint. | AC-TRACK-1..7/9/10, AC-TRACK-8 on a seeded `agent_matches` row, AC-MAIL-1/2/3/5, AC-REM-*, AC-PROP-1 (engagement clauses)/3, AC-IP-3/8, AC-SEC-6/7 pass; E2E happy path reaches Closed. |
| **4 Scout agents** | Scout config form with preview; pipeline; digests (N02/EM3); feedback loop; cost ledger and caps; injection suite. | AC-SCOUT-*, AC-TRACK-8 (real digest), the AC-REPO-4 digest and AC-DIR-5 scout-form clauses pass; injection eval gates pass on cassettes; G-EVAL signed. |
| **5 Research, trending, personalisation** | Research agent with moderation queue; trending + Opportunity Gap; hybrid ranker with impressions log and pursuit recommendation; LTR pipeline in shadow mode; offline eval set. | AC-RES-*, AC-TREND-*, AC-PERS-*, the AC-REPO-6 trending clause pass; EM7 trending line unflagged. |
| **6 Subscriptions & billing** | Payment rails: Daraja + Paystack sandbox, callback verification, invoices/receipts (eTIMS), grace/downgrade, admin refunds; G3 price values. | AC-SUB-* pass; security-reviewer PASS; live keys untouched until G4. |
| **7 UX consolidation** | Nav audit, empty states, onboarding, 360 px, Swahili keys, performance budgets, Data-saver. | AC-UX-* pass; ux-reviewer PASS. |
| **8 Hardening & launch readiness** (Fable audit) | Full security review, scanners, load test on digest/reminder workers, backups + restore drill, observability, DSR endpoints, legal placeholders replaced (G2), brand (G5), email DNS (G7), staging deploy, runbooks, admin console complete, dispute evidence packs. | No BLOCKER/MAJOR open; AC-SEC-*, AC-ADM-*, AC-IP-7 pass; `docs/spec/14-definition-of-done.md` E2E on staging; G2, G5, G6, G7 signed; report submitted for G8. |

**Releases.** "Phase N" means only build phases 0–8; product releases are R1/R2/R3, recorded per row in `REQUIREMENTS.md` (nothing dropped, only scheduled). **Release 1** = everything this build delivers before G8 (the anchor-org pilot); it launches with free and anchor plans plus manual invoicing, and live Daraja/Paystack/eTIMS once G4 is signed (may follow launch). **Release 2:** remaining niches seeded, county-level research, invisible watermark marks, OpenTimestamps, KYC vendor (D2 automated), E-CSP advanced e-signature, GitHub App investigator, WhatsApp reminders and adviser, co-owned proposals, SSO, M-Pesa Ratiba, pipeline analytics, offline tracker, similarity monitor, Swahili live, local companion `--platform`. **Release 3:** LTR activation if not yet reached, East Africa (Uganda, Tanzania, Rwanda: MTN MoMo, Airtel Money, local registries), success fees/escrow only via a CBK-licensed PSP partner, public API, procurement-friendly government flows. **Explicit non-goals:** holding project funds, auto-emailing unclaimed orgs, "theft-proof" claims, black-box ranking, native mobile apps (responsive PWA instead), regions outside Kenya in R1.
