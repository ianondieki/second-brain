"""Entry point for ``python -m bridge.seed``. Filled in by T1.4 (niches, plans, holidays, regions)."""

from __future__ import annotations

import sys


def main() -> int:
    print("seed: nothing to seed yet (reference tables arrive with the first migration, T1.4)")
    return 0


if __name__ == "__main__":
    sys.exit(main())
