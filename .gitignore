# Secrets
.env
*.key

# Local runtime / volumes if ever bind-mounted into repo
n8n_data/
evolution_instances/
postgres_data/
redis_data/

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Personal vault content (keep only templates in git; archived under legacy/)
legacy/notes/projects/*
legacy/notes/learning/*
!legacy/notes/projects/.gitkeep
!legacy/notes/learning/.gitkeep

# Project reminders: live settings, state, logs, previews
reminders.json
.state/
__pycache__/
*.pyc
.venv/

# WhatsApp adviser: the cloudflared binary (download it; ~55 MB)
tools/*.exe
graphify-out/

# The legacy suite creates a literal C: directory when it runs on Linux (docs/spec/02-existing-repo.md)
/C:/
